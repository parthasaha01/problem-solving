#include<bits/stdc++.h>
using namespace std;
#define ll long long
ll n, ans=0;
vector<ll>a,pos,neg;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    cin>>n;

    for(int i=0; i<n; i++){
        cin>>x;
        v.push_back(x);
        if(x<0)neg.push_back(x);
        else pos.push_back(x);
    }

    sort(v.begin(), v.end());


    //main();

    return 0;

}


