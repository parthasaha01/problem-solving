#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);

    int tt; cin>>tt;
    for(int ks=1; ks<=tt; ks++)
    {
        int n,q; cin>>n>>q;
        int ans = 0;
        while(q--)
        {
            int t,x,y; cin>>t>>x>>y;

            if(x>n||y>n){
                ans++;
                continue;
            }

            if(t==1)
            {

            }
            else
            {

            }
        }
    }
}
