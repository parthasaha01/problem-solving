#include<bits/stdc++.h>
using namespace std;
char st[15];
int n,x;
int vs[300005];
int main()
{
    scanf("%d",&n);
    int nn=2*n;
    for(int i=1; i<=nn; i++)
    {
        scanf("%s",st);
        //printf("%s\n",st);

        if(st[0]=='a')
        {
            scanf("%d",&x);
        }
        else if(st[0]=='r')
        {

        }
    }
}
